# 导航栏与功能按钮映射说明

> 本文档供组员快速定位自己负责的页面/按钮，以及如何在现有框架下添加功能入口。

---

## 一、底部导航栏（4个Tab）

底部导航栏位于 `1_CampusApp_Flutter/lib/features/home/home_page.dart`，由 `_HomePageState._currentIndex` 控制切换。

| Tab | 名称 | 图标 | 对应功能模块 | 负责人 |
|---|---|---|---|---|
| 0 | 首页 | `home_filled` | 全局聚合入口、Banner、金刚区 | 周玮（UI框架） |
| 1 | 地图导览 | `explore_outlined` | 3.1.2 地图展示 + 3.2.4 智能路线规划 | 李卓尔（地图）、（路线算法） |
| 2 | 智能讲解 | `headphones_rounded` | 3.1.3 实时定位 + 3.1.4 地理围栏触发 + 3.1.5 多媒体讲解 | （定位）、周玮（围栏+播报） |
| 3 | 我的 | `person_outline_rounded` | 3.1.1 用户管理 + 3.2.5 打卡徽章 | 李卓尔（用户）、周玮（徽章） |

### 如何修改Tab对应的页面

在 `home_page.dart` 的 `IndexedStack` 中，每个Tab对应一个独立的 Widget 类：

```
_index=0 → _TabHome()     （首页）
_index=1 → _TabMapGuide() （地图导览）
_index=2 → _TabSmartAudio()（智能讲解）
_index=3 → _TabProfile()  （我的）
```

修改对应 `_TabXxx` 类即可。注意每个类都在同一个 `home_page.dart` 文件中（方便联调），后续可拆分到独立文件。

---

## 二、首页金刚区（8个快捷按钮）

金刚区位于 `_TabHome._buildGridNav()` 方法中，8个按钮及其行为：

| 按钮 | 图标 | 点击行为 | 负责同学 |
|---|---|---|---|
| **校园地图** | `map_outlined` | 切换到 Tab 1（地图导览） | 李卓尔 |
| **路线规划** | `route_outlined` | 切换到 Tab 1（地图导览）→ 唤起路线选择菜单 | 周玮 |
| **AR 扫一扫** | `view_in_ar_rounded` | `Navigator.pushNamed('/ar')` → AR识别页面 | 陈昱霖 |
| **景点打卡** | `workspace_premium_outlined` | 切换到 Tab 3（我的）→ 展示打卡列表 | 周玮 |
| **校园故事** | `auto_stories_outlined` | `Navigator.pushNamed('/checkin')` → 打卡徽章页面 | 周玮 |
| **校车时刻** | `directions_bus_filled_outlined` | 暂无跳转，待实现 | 待定 |
| **离线下载** | `cloud_download_outlined` | 暂无跳转，待实现 | 李卓尔 |
| **校园公告** | `campaign_outlined` | 暂无跳转，待实现 | 待定 |

### 添加新按钮

在 `_buildGridNav()` 的 `Wrap` children 中添加一个新的 `_GridButton`：

```dart
_GridButton(
  icon: Icons.your_icon,
  label: '按钮名',
  onTap: () {
    // 跳转到你的页面
  },
),
```

---

## 三、全局悬浮球：西小导 AI（FAB）

- **位置**：右下角常驻悬浮球
- **代码位置**：`_HomePageState._buildXiXiaoDaoFab()` → 点击唤起 `_showChatSheet()`
- **功能**：对应 3.2.2 AI虚拟导游与多轮问答模块
- **负责人**：陈昱霖
- **当前状态**：弹出了半屏对话面板（毛玻璃UI），但对话逻辑为空
- **待实现**：在 `_showChatSheet()` 的 `Container` 中接入聊天UI组件（已有 `chat_page.dart` 可复用），对接 Python RAG 接口

---

## 四、顶部搜索栏

- **代码位置**：`_TabHome._buildTopSearchBar()`
- **当前状态**：纯展示，点击无反应
- **待实现**：点击后跳转搜索页面或弹出搜索弹窗
- **负责人**：待定

---

## 五、首页 Banner

- **代码位置**：`_TabHome._buildBanner()`
- **当前状态**：静态渐变色占位块
- **待实现**：接入轮播图组件（如 `PageView` + 自动滚动），展示校园风光图片
- **负责人**：贾丝楠（内容提供图片素材）、周玮（UI实现）

---

## 六、首页推荐卡片

- **代码位置**：`_TabHome._buildRecommendCard()`
- **当前状态**：静态展示两条推荐建筑
- **待实现**：对接后端热门景点接口，动态加载
- **负责人**：贾丝楠（后端接口）、周玮（Flutter UI）

---

## 七、智能讲解页（Tab 2）详细结构

这是最复杂的页面，包含三个叠层：

### 7.1 地图底图层
- **代码位置**：`_TabSmartAudio.build()` 的 `Stack` 第一层
- **当前状态**：占位图标+文字
- **待实现**：集成高德地图 Flutter SDK，显示实时GPS蓝点
- **负责人**：李卓尔

### 7.2 地理围栏开关条
- **代码位置**：`Positioned(top: 16, ...)` 毛玻璃横条
- **当前状态**：UI已完成，Switch 可切换
- **待实现**：对接定位服务 `location_service.dart`，计算用户与景点距离，进入50米范围触发播放
- **负责人**：周玮（围栏逻辑）、李卓尔（GPS数据源）

### 7.3 底部音频播放控制器
- **代码位置**：`Positioned(bottom: 110, ...)` 毛玻璃卡片
- **当前状态**：UI已完成，播放按钮有触感反馈，但未接入真实音频
- **待实现**：对接 TTS 接口获取音频流，播放/暂停切换，进度条
- **负责人**：周玮

---

## 八、路由总览

所有页面路由在 `lib/core/router/app_router.dart`：

| 路由路径 | 页面类 | 说明 | 负责人 |
|---|---|---|---|
| `/home` | `HomePage` | 4Tab首页框架 | 周玮 |
| `/login` | `LoginPage` | 登录页（当前跳过验证） | 李卓尔 |
| `/register` | `RegisterPage` | 注册页 | 李卓尔 |
| `/profile` | `ProfilePage` | 个人中心独立页 | 李卓尔 |
| `/map` | `MapPage` | 地图全屏页（备用） | 李卓尔 |
| `/spot/detail` | `SpotDetailPage` | 景点详情 | 贾丝楠 |
| `/guide` | `GuidePage` | 语音讲解独立页 | 周玮 |
| `/chat` | `ChatPage` | AI对话独立页 | 陈昱霖 |
| `/ar` | `ARPage` | AR识别页 | 陈昱霖 |
| `/route` | `RoutePage` | 路线规划独立页 | 周玮 |
| `/checkin` | `CheckinPage` | 打卡徽章页 | 周玮 |

### 如何从任意按钮跳转到指定页面

```dart
// 方式1：命名路由跳转（推荐用于跨Tab页面）
Navigator.of(context).pushNamed('/chat');

// 方式2：切换底部Tab（在_HomePageState内部或通过findAncestorStateOfType）
// 参考 _TabHome._switchTab() 方法
```

---

## 九、Flutter项目结构速查

```
lib/
├── main.dart                          # 入口，设 initialRoute
├── core/
│   ├── theme/app_theme.dart           # 全局色板（primary/darkBlue/textMain等）
│   ├── router/app_router.dart         # 所有路由注册
│   ├── network/network_client.dart    # HTTP客户端
│   └── storage/local_storage.dart     # SQLite离线存储
├── features/
│   ├── home/home_page.dart            # ★ 4Tab首页（本文件）
│   ├── user/login_page.dart           # 登录
│   ├── user/register_page.dart        # 注册
│   ├── user/profile_page.dart         # 个人中心
│   ├── map/map_page.dart              # 地图
│   ├── location/location_service.dart # GPS定位
│   ├── cache/cache_service.dart       # 离线缓存
│   ├── spot/spot_detail_page.dart     # 景点详情
│   ├── guide/guide_page.dart          # 语音讲解
│   ├── chat/chat_page.dart            # AI对话
│   ├── ar/ar_page.dart                # AR识别
│   ├── route/route_page.dart          # 路线规划
│   └── social/checkin_page.dart       # 打卡徽章
└── shared/widgets/
    └── glass_card.dart                # 毛玻璃卡片组件
```

---

## 十、下一步开发建议

1. **李卓尔**：在 `_TabMapGuide` 中集成高德地图 SDK → 实现 `location_service.dart` 的真实GPS回调 → 在 `_TabSmartAudio` 中对接定位数据
2. **周玮**：实现地理围栏距离计算（Java Redis）→ 实现音频播放器控制逻辑 → 路线规划算法
3. **陈昱霖**：在 `_showChatSheet()` 中嵌入聊天UI → 对接 Python RAG 接口 → AR 识别集成
4. **贾丝楠**：提供校园风光图片（Banner）+ 景点数据录入后台 → 在 `_TabHome._buildRecommendCard()` 中接入动态数据

---

> **最后更新**：2026-05-23
> **说明**：此文档基于 `home_page.dart` 的实际代码编写，所有按钮和跳转行为均可直接对照代码查看。如有新增功能入口，请同步更新此文档。
